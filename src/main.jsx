import React from "react";
import ReactDOM from "react-dom/client";
import FrotaTechApp from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FrotaTechApp />
  </React.StrictMode>
);
